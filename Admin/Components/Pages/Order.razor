@page "/order"
@layout Admin.Components.Layout.MainLayout

<div class="page-container">
    <h2 class="page-title">📦 Orders</h2>

    <table class="order-table">
        <thead>
            <tr>
                <th>Order ID</th>
                <th>Customer</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in orders)
            {
                <tr>
                    <td>@order.Id</td>
                    <td>@order.Customer</td>
                    <td>
                        <select @bind="order.Status" class="status-select">
                            <option>Pending</option>
                            <option>Picked Up</option>
                            <option>Delivered</option>
                        </select>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    private List<OrderModel> orders = new()
    {
        new() { Id = "ORD-001", Customer = "Anna Cruz", Status = "Pending" },
        new() { Id = "ORD-002", Customer = "Marco Diaz", Status = "Picked Up" },
        new() { Id = "ORD-003", Customer = "Jessa Flores", Status = "Delivered" },
    };

    public class OrderModel
    {
        public string Id { get; set; } = "";
        public string Customer { get; set; } = "";
        public string Status { get; set; } = "";
    }
}

<style>
.page-container {
    padding: 2rem;
    color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    height: 100%;
    margin: 0;
    overflow-y: auto;
    background: rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(10px);
    background-image: url('/images/water-bg.jpg');
    background-size: cover;
    background-position: center;
}

.page-title {
    margin-bottom: 1.5rem;
    font-size: 1.8rem;
}

.order-table {
    width: 90%;
    max-width: 1000px;
    border-collapse: collapse;
    text-align: center;
    background: rgba(255,255,255,0.85);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    margin: 0;
}

.order-table th, .order-table td {
    padding: 1rem;
    color: #002f5f;
}

.order-table th {
    background: #007acc;
    color: white;
    font-weight: bold;
}

.status-select {
    border: none;
    padding: 0.4rem 0.8rem;
    border-radius: 8px;
    background: #e3f3ff;
    color: #003366;
    font-weight: bold;
}
</style>