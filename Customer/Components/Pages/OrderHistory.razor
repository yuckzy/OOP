@page "/order-history"
@inherits LayoutComponentBase

<div class="page-container">
    <h2 class="page-title">⏰ Order History</h2>

    <div class="user-info">
        👤 <strong>Welcome, @CurrentUser!</strong>
        <span class="role-badge customer">Customer</span>
    </div>

    <table class="order-table">
        <thead>
            <tr>
                <th>Customer Name</th>
                <th>Gallon Type</th>
                <th>Quantity</th>
                <th>Total</th>
                <th>Status</th>
                <th>Out for Delivery</th>
                <th>Time Delivered</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in orders)
            {
                <tr>
                    <td>@order.CustomerName</td>
                    <td>@order.GallonType</td>
                    <td>@order.Quantity</td>
                    <td>₱@order.Total.ToString("N2")</td>
                    <td>
                        <span class="@GetStatusClass(order.Status)">@order.Status</span>
                    </td>
                    <td>@(order.OutForDelivery?.ToString("yyyy-MM-dd HH:mm") ?? "—")</td>
                    <td>@(order.TimeDelivered?.ToString("yyyy-MM-dd HH:mm") ?? "—")</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    private string CurrentUser = "Aljiemer Ting";
    private List<OrderModel> orders = new();

    protected override void OnInitialized()
    {
        var allOrders = new List<OrderModel>
        {
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Round Gallon",
                Quantity = 2,
                Total = 240.00m,
                Status = "Pending",
                OutForDelivery = null,
                TimeDelivered = null
            },
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Slim Gallon",
                Quantity = 1,
                Total = 120.00m,
                Status = "Picked Up",
                OutForDelivery = new DateTime(2025, 10, 2, 11, 30, 0),
                TimeDelivered = null
            },
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Round Gallon",
                Quantity = 3,
                Total = 360.00m,
                Status = "Delivered",
                OutForDelivery = new DateTime(2025, 9, 30, 9, 15, 0),
                TimeDelivered = new DateTime(2025, 9, 30, 10, 15, 0)
            },
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Slim Gallon",
                Quantity = 2,
                Total = 240.00m,
                Status = "Cancelled",
                OutForDelivery = null,
                TimeDelivered = null
            },
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Round Gallon",
                Quantity = 1,
                Total = 120.00m,
                Status = "Pending",
                OutForDelivery = null,
                TimeDelivered = null
            },
            new() {
                CustomerName = "Aljiemer Ting",
                GallonType = "Slim Gallon",
                Quantity = 4,
                Total = 480.00m,
                Status = "Picked Up",
                OutForDelivery = new DateTime(2025, 10, 3, 14, 20, 0),
                TimeDelivered = null
            }
        };

        // Auto-calculate delivered time for delivered orders
        foreach (var o in allOrders)
        {
            if (o.Status == "Delivered" && o.OutForDelivery.HasValue && !o.TimeDelivered.HasValue)
            {
                o.TimeDelivered = o.OutForDelivery.Value.AddHours(1);
            }
        }

        // Sort by status order: Pending -> Picked Up -> Delivered -> Cancelled
        orders = allOrders.OrderBy(o => o.Status switch
        {
            "Pending" => 1,
            "Picked Up" => 2,
            "Delivered" => 3,
            "Cancelled" => 4,
            _ => 5
        }).ToList();
    }

    private string GetStatusClass(string status) => status switch
    {
        "Delivered" => "status-delivered",
        "Picked Up" => "status-pickedup",
        "Pending" => "status-pending",
        "Cancelled" => "status-cancelled",
        _ => ""
    };

    public class OrderModel
    {
        public string CustomerName { get; set; } = "";
        public string GallonType { get; set; } = "";
        public int Quantity { get; set; }
        public decimal Total { get; set; }
        public string Status { get; set; } = "";
        public DateTime? OutForDelivery { get; set; }
        public DateTime? TimeDelivered { get; set; }
    }
}

<style>
.page-container {
    padding: 2rem;
    color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    height: 100%;
    margin: 0;
    overflow-y: auto;
    background: rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(10px);
    background-image: url('/images/water-bg.jpg');
    background-size: cover;
    background-position: center;
}

.page-title {
    margin-bottom: 1rem;
    font-size: 1.8rem;
}

/* Logged-in user info */
.user-info {
    margin-bottom: 1.5rem;
    background: rgba(255,255,255,0.15);
    padding: 0.8rem 1.5rem;
    border-radius: 10px;
    font-size: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.role-badge {
    padding: 0.3rem 0.8rem;
    border-radius: 8px;
    font-weight: bold;
    font-size: 0.85rem;
}

.role-badge.customer {
    background: #28a745;
    color: white;
}

.order-table {
    width: 90%;
    max-width: 1100px;
    border-collapse: collapse;
    text-align: center;
    background: rgba(255,255,255,0.9);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    margin: 0;
}

.order-table th, .order-table td {
    padding: 1rem;
    color: #002f5f;
}

.order-table th {
    background: #007acc;
    color: white;
    font-weight: bold;
    text-transform: uppercase;
}

.order-table tr:nth-child(even) {
    background: rgba(0, 0, 0, 0.03);
}

.order-table tr:hover {
    background: rgba(0, 122, 204, 0.08);
}

/* Status Styles */
.status-delivered {
    color: #155724;
    background: #d4edda;
    padding: 0.3rem 0.8rem;
    border-radius: 8px;
    font-weight: bold;
}

.status-pickedup {
    color: #856404;
    background: #fff3cd;
    padding: 0.3rem 0.8rem;
    border-radius: 8px;
    font-weight: bold;
}

.status-pending {
    color: #0d6efd;
    background: #cfe2ff;
    padding: 0.3rem 0.8rem;
    border-radius: 8px;
    font-weight: bold;
}

.status-cancelled {
    color: #721c24;
    background: #f8d7da;
    padding: 0.3rem 0.8rem;
    border-radius: 8px;
    font-weight: bold;
}
</style>